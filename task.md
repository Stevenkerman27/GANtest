我希望修改test_cgan.py, 更全面地评估训练效果。分析如下方案是否合理
对于预训练和PG训练的结果，各生成N个翼型(不写入硬盘)，工况在config中的迎角和雷诺数范围内拉丁超立方采样获得，相对厚度和升力系数在数据集范围内拉丁超立方采样获得。调用xfoil计算生成的翼型的相对厚度误差和升力系数误差并绘图，总共四张2D图，分别对应预训练和PG训练的相对厚度误差和升力系数误差，xy轴分别为迎角和雷诺数，计算(误差+方差)以代表这个工况处生成翼型的“不准确度”, 在2D图中用颜色画出不准确度。随后在硬盘中写入N个翼型中的m个误差最小的翼型。